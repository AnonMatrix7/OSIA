==OSIA==

This is an example model for collaborating to create the OSIA documents. Resources files are composed using a very simplistic plain-text format, which can then be processed by custom tools to generate sanitized media formats, such as HTML. 
